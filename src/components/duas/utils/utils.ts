import { FilterType } from "@/components/duas/types/types";

export const FILTERS: { key: FilterType; label: string }[] = [
  { key: "all", label: "All" },
  { key: "favorites", label: "Favorites" },
];